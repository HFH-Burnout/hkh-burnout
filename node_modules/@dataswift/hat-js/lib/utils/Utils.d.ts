import { HatHttpParameters } from '../interfaces/http.interface';
export declare class Utils {
    static buildRequestURL: (url: string, params?: HatHttpParameters | undefined) => string;
    static getProtocol: (ssl?: boolean | undefined) => "http://" | "https://";
}
