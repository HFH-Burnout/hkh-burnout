export interface HatRegistration {
    generateBaasSignupUrl: (applicationId: string, hatName: string, email: string, redirect: string, newsLetterOptins?: boolean) => string;
    generateDaasSignupUrl: (applicationId: string, redirect: string, tags?: string[]) => string;
}
export declare const registration: () => HatRegistration;
